[project]
name = "gymtracker-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi",
  "uvicorn",
  "sqlalchemy[asyncio]",
  "asyncpg",
  "pydantic-settings",
  "python-dotenv",
  "psycopg2-binary",
  "alembic"
]

[tool.setuptools]
packages = []